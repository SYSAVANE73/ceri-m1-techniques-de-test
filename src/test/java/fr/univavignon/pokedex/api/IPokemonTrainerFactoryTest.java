package fr.univavignon.pokedex.api;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

import org.junit.Test;

public class IPokemonTrainerFactoryTest {

	
	@Test
	public void createTrainer() {
		/*
		IPokemonTrainerFactory em = mock(IPokemonTrainerFactory.class);
		Team team = null;
		IPokedexFactory pokedex = mock(IPokedexFactory.class);
		IPokemonMetadataProvider pokprovider = mock(IPokemonMetadataProvider.class);
		IPokemonFactory pokemonFactory = mock(IPokemonFactory.class);
		
		IPokedex poke = mock(IPokedex.class);
		
		pokemonFactory.createPokemon(0, 613, 64, 4000, 4);
		pokprovider.getPokemonMetadata(0);
		pokedex.createPokedex(pokprovider, pokemonFactory);
		
		
		PokemonTrainer pokTrainer = new PokemonTrainer("Bulbizarrz", team.valueOf("MYSTIC"), pokedex);
		
		when(em.createTrainer("Bulbizarrz", team.valueOf("MYSTIC"), pokedex)).thenReturn(pokTrainer);
		when(em.getPokemonMetadata(133)).thenReturn(pok2);
		
		assertEquals(em.getPokemonMetadata(133), pok3);
		*/
	}
	
}
